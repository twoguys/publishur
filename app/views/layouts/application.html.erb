<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
       "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title>Publishur</title>
	<link rel="shortcut icon" href="<%= root_path() %>favicon.png" />
	<%= javascript_include_tag 'jquery-1.3.2.min' %>
	<%= javascript_include_tag 'application' %>
  <%= stylesheet_link_tag 'app' %>
	<%= yield :head %>
	
	<script type="text/javascript">
		$(document).ready(function() {
			// startup
		});
	</script>
</head>

<body>
	<div id="login">
		<div id="login-insides">
			<ul>
				<% if current_user %>
				<li>Signed in as <%= current_user.email %></li>
				<li class="last"><%= link_to('Sign out', signout_path) %></li>
				<% else %>
				<li><%= link_to('Sign up', signup_path) %></li>
				<li class="last"><%= link_to('Sign in', signin_path) %></li>
				<% end %>
			</ul>
		</div>
	</div>
	<div id="header">
		<div id="header-insides">
			<%= link_to(image_tag('/images/logo.gif'), root_path) %>
			<ul id="nav">
				<li class="first">
					<%= link_to_active('Home', root_path, @nav == :home) %>
				</li>
				<% if current_user %>
				<li>
					<%= link_to_active('Dashboard', dashboard_path, @nav == :dashboard) %>
				</li>
				<li>
					<%= link_to_active('Groups', groups_path, @nav == :groups) %>
				</li>
				<li class="last">
					<%= link_to_active('Settings', root_path, @nav == :settings) %>
				</li>
				<% else %>
				<li>
					<%= link_to_active('Tour', root_path, @nav == :tour) %>
				</li>
				<li>
					<%= link_to_active('Prices', root_path, @nav == :prices) %>
				</li>
				<li class="last">
					<%= link_to_active('Try it!', signup_path, @nav == :signup) %>
				</li>
				<% end %>
			</ul>
		</div>
  </div>
  <div id="centered">
		<div id="mid">
			<div id="notice">
				<%= render 'layouts/flashes' %>
			</div>
			<%= yield :wide %>
			<div id="mid-insides">
				<%= yield %>
			</div>
		</div>
	</div>
	<div id="footer">
		<table id="footer_insides">
			<tr>
				<td>
					What is Publishur?
				</td>
				<td>
					How does it work?
				</td>
				<td>
					This is a <%= link_to('Two Guys', 'http://weretwoguys.com') %> project
				</td>
			</tr>
		</table>
	</div>
</body>	
</html>